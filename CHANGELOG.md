# Changelog

## [Unreleased]

## [4.2.1] - 2026-09-07

> [!WARNING]
> 本次更新调整卡片多选、拖放和图像处理等多个流程，仍可能存在尚未发现的问题。如遇异常，请通过“设置 → 关于 → 导出诊断日志”反馈。

### ✨ 新增

- “设置 → 隐私与分享”新增“复制/拖拽时添加水印”，使用已保存的默认水印生成输出副本，不修改原图；开关默认关闭，可独立于保护模式使用。添加水印不会清除元数据，需要清除时请同时启用保护模式和对应的元数据清除选项。
- Android 支持从 Discord 等应用分享单张图片或图片直链到 NAI Launcher，再选择导入参数、图生图或参考用途；直链须可访问，恢复生成参数需要保留元数据的原图。
- 本地图库、Vibe 库、精准参考库和词库的常驻侧栏支持拖动调整宽度，并分别在当前设备记住设置。
- 文件夹、相簿及资源分类侧栏新增原有顺序、名称和数量排序；支持拖动调整顺序，适用的树形分类可调整层级。各区域分别记住本机排序，手动重排后切换为原有顺序。

### 🛠 改进

- 统一图库、生成预览与历史、Vibe 库、精准参考库和词库的卡片操作：桌面支持 Ctrl/Cmd 切换选中、Shift 扩选、Ctrl/Cmd+A 选择当前展示项和 Esc 退出，触屏保留长按多选与更多菜单；选中项的菜单执行支持的批量操作，未选中项仍只操作自身。
- 桌面拖动选中卡片时可带上整组选中资源，导出真实图片、Vibe/Bundle 或精准参考配置包；接收端检查整组类型与数量，单项入口会明确拒绝多项，批量处理会报告实际成功和失败情况。
- Discord 分享提交后可在任务提示中查看进度与结果，部分发送失败时仅重试失败目标；遇到限流显示等待时间，不自动重复发送。
- DLSSNR 自动增强按准备、增强和收尾阶段显示中央状态提示，完成前保持流式预览连续；失败时保留原图并提示手动重试。减少重复解码与图像数据搬运，并将预设和参数整理在同一分组内。
- DLSSNR 手动增强面板的“另存增强图像”会把结果作为最新生成结果加入历史记录、同步图库索引并显示在生成页；文件仍按原图种子命名，不覆盖原图。
- 移除图生图“放大”中的独立 DLSS SR 入口，VSR 仅作为 DLSSNR 增强流程的组成部分使用。
- 优化生成按钮与额度、批次工具的自适应布局；空间不足时主按钮排在工具下方，大字号下取消和跳过批次仍可操作。
- 收紧手机提示词底栏的工具间距，改善概览状态和角色标签对齐；横向操作栏补充两侧滚动提示。
- 账号摘要新增本地化的订阅到期日期，并优化账号资料面板的内容高度、对齐及窄屏操作空间。
- 智能体回复中的代码块支持快捷复制，资源引用与工具结果说明更清晰；导出诊断日志包含脱敏的权限审计线索，便于排查执行问题。

### 🐛 修复

- 修复本地图库收藏筛选未完整结合其他条件，以及从相簿切换到收藏后仍残留原相簿条件的问题。
- 修复词库混合导入时跳过、重命名和覆盖方案相互影响的问题，按条目落实对应方案，并避免重命名导入覆盖本地原有预览图。
- 修复持续动画可能阻止自动检查更新的问题；应用在前台定期检查，后台暂停并在返回后补查，同时改善更新提示与安装确认在手机、大字号下的布局。
- 修复手机自动补全候选框与软键盘、输入光标位置不一致的问题；空间较小时优先保证候选项可见，并随输入区域滚动调整位置。
- 修复 PNG 清除元数据时的图像处理问题，保留 16 位图像精度与 APNG 动画帧、时序和循环信息；改善复制、拖拽准备与临时文件释放，避免接收端尚未读完文件就被清理。
- 修复智能体 Responses 推理参数与图片工具结果的协议兼容问题；连接建立阶段的短暂网络故障可有限重试，已收到响应内容后不会重复回放请求。
- 修复侧栏拖动结束后焦点高亮残留、卡片选择时悬浮预览遮挡操作，以及 Windows 键被误当作多选修饰键的问题。

## [4.2.0] - 2026-09-06

> [!WARNING]
> 本次更新新增 Windows DLSS 图像处理，并调整智能体交互与图片展示，仍可能存在尚未发现的问题。如遇异常，请通过“设置 → 关于 → 导出诊断日志”反馈。

### ✨ 新增

- Windows 新增 DLSSNR 本地图像增强：在“设置 → 集成 → DLSSNR”按需安装运行库并检测 NVIDIA GPU，然后从图片卡片或图生图增强入口调整效果、对比原图并保存独立副本；处理不消耗 Anlas，保留透明通道和支持的生成元数据，不覆盖原图。
- DLSSNR 支持生成后自动增强，默认关闭；开启后在记录生成结果与自动保存前完成增强，处理失败或在增强阶段取消时保留已生成的原图。
- DLSSNR 提供 7 套内置预设，默认使用“质感光影”，可切换“保色增强”等风格，也可另存、重命名和删除自定义预设。支持数值输入与参数说明，当前选择和调整自动保存。
- 图生图“放大”新增 DLSS SR 入口；SR 倍率默认 2 倍，可手动输入。该入口的最终图片不混入 NR 效果，但当前运行库仍会执行一次 NR，需通过完整运行环境检测。
- 智能体新增逐题回答的问题表单，每题提供三个建议方向及自定义回答，支持返回修改并在提交前检查。等待时显示入口状态和提醒，Android 可通过系统通知返回问题；默认等待两分钟，到期会采用全部推荐项，覆盖尚未提交的草稿选择，超时不代表授权付费或删除操作。

### 🛠 改进

- 图片前后对比支持可选的“跟随鼠标”，并在本机记住偏好；放大后可拖动画面检查局部，支持原尺寸查看与适应窗口，重新处理时保持查看位置。
- 智能体系统提示词编辑明确区分“补充要求”和“替换正文”，支持自然语言直接编辑；工作目录、联网状态、可用 Skills 与执行规则自动补充，设置预览与实际发送内容一致，恢复默认不会切换编辑模式。
- 完全访问模式下，智能体对已准备且确认费用为零的生成和重绘可直接提交，减少重复确认；付费、未知费用和破坏性操作继续使用权限审批，并改善确认卡片的文字对比度。
- 完善智能体默认角色调查指引，分别核实角色身份、标准标签与画廊外观证据，并尊重联网关闭设置；工具结果摘要更清楚地展示来源、实际返回数量和截断信息。
- 生成模型、智能体、服务商设置和相关图片信息增加随应用提供的品牌图标，无需联网加载，未知模型仍有通用图标。
- 图片查看区域统一使用中性深色背景，减少主题色对观图的干扰；透明棋盘与用户选择的图片背景仍按原设置显示。
- 图片快捷菜单改为均衡的双列布局；优化任务进度、顶部更新提示与消息提示的呈现，减少页面跳动，并区分成功等状态。
- 优化手机提示词编辑与工具栏空间：标签编辑显示铅笔标记，编辑时暂时收起译文；改善取消选择、编辑入口和光标命中区域，移除提示词助手多余底部留白，并为横向工具栏补充滚动提示。
- 收紧手机生成页额度栏与图片信息条的留白，减少种子和透明背景操作区域多余的横向滚动。

### 🐛 修复

- 修复流式生成结束切换到最终图片时的闪烁，最终图片解码完成前继续显示已有预览。
- 修复智能体停止后模型网络请求未同步中止的问题。
- 修复手机智能体返回和键盘焦点处理，返回时优先收起输入焦点，点击空白处可退出输入状态。
- 修复本地图库悬浮预览沿用生成元数据尺寸的问题，改为显示图片实际像素尺寸。
- 修复图片菜单关闭后鼠标高亮残留，以及触屏与鼠标切换后快捷操作未正确恢复的问题，同时保留键盘焦点操作。

## [4.1.0] - 2026-09-06

> [!WARNING]
> 本次更新涉及提示词编辑与云备份格式等较多调整，仍可能存在尚未发现的问题。如遇异常，请通过“设置 → 关于 → 导出诊断日志”反馈。
>
> **云备份兼容性：** 新版本可以读取旧备份，但旧版本不能读取本版生成的新格式备份；请先更新需要同步的设备，再推送新备份。Google Drive 新连接目前仍因 OAuth 审核暂不可用，界面会明确提示。

### ✨ 新增

- 主提示词、角色词、固定词和词库编辑器新增统一的文本／标签模式切换。标签模式可直接查看本地中文译文、编辑标签、拖拽排序、框选或多选，并保留权重分组与嵌套强调关系；各编辑区独立记住模式与手动高度。
- 提示词支持单个或批量启用、禁用、删除与成组调权，文本选区也可使用对应工具栏和右键操作。禁用内容会保留在编辑器中，但不会计入有效提示词或发送给生成服务；向旧版本或其他工具粘贴时，请使用“复制有效提示词”。
- 新增图片打码编辑器，可从图片操作入口创建马赛克、模糊或纯色遮挡，支持区域移动与缩放、画笔、反选以及撤销／重做；保存为独立副本，保留原图。“设置 → 安全与分享”可编辑默认打码样式，导出默认移除元数据，也可按需保留。
- 提示词助手新增按服务商配置的自动／手动并发控制，并可分别为优化、翻译、反推、角色替换和自定义改写选择模型支持的思考等级；自动模式会根据请求结果调整并发，遇到限流时按服务端要求等待重试。

### 🛠 改进

- 提示词助手统一为编辑区内的展开工具栏，提供更清晰的执行、取消与进度反馈，点击编辑区外可收起；标签翻译优先使用本地词库，只将缺失译文交给助手，补充标签说明时不改写原提示词。
- 完善提示词权重编辑、整组选择与撤销操作，权重恢复为 1 时自动解除对应分组；编辑器底部操作随可见区域保持可达，模式切换、自动高度、角色高度拖动与内外滚动更加连贯。
- 减少长提示词编辑和翻译时的重复处理，固定词与分组资料网格改为按需构建；改进固定词选中状态、文字对比度以及大字号下的标题布局。
- 词库选择卡片支持共享的原文与中文悬浮预览，长译文按内容展开，并改善选择按钮的对比度；词库、固定词和角色等编辑入口的操作尺寸与浮层风格更加一致。
- 云备份合并传输小型数据对象，减少大量零散请求，同时继续复用未变化内容并支持旧备份恢复；改善 OneDrive 新建同步目录与连接恢复体验。
- 减少图像生成准备、流式预览和图生图放大时的重复复制与图片解码；本地图库批量标签、收藏操作和智能代理会话列表加载也减少了重复读取。
- 快捷键录制可识别更多符号键，设置与帮助中的动作名称保持一致，并补齐 Vibe 操作的多语言说明；设置侧栏的不同长度标签保持统一对齐。
- 统一提示词语义配色、输入框与工具按钮的层级，减少常态背景色块，并延后加载非当前主题字体，降低启动阶段的额外工作。

### 🐛 修复

- 修复本地图库未输入搜索词时部分高级筛选不生效，以及文件列表数量相同但内容变化后仍复用旧筛选结果的问题；筛选为空时显示“无匹配结果”和“清除筛选”，不会再误报图库没有图片。
- 修复生成请求在读取或编码图片的准备阶段可以被重复提交的问题；准备开始即显示忙碌与取消反馈，取消后不会继续提交旧请求。
- 修复保存云连接后的前台检查可能阻塞首次手动备份的问题；手动操作会等待连接检查完成，并明确反馈检查错误。
- 修复紧凑提示词区域在极小高度下溢出，以及权重工具栏关闭后仍有延时回调的问题；受限空间下仍可滚动访问编辑内容与底部操作。
- 加强词库导入包的条目、成员路径与预览图校验，拒绝路径越界、重复成员和不合法的缩略图引用。

## [4.0.2] - 2026-09-05

### ✨ 新增

- “设置 → 提示词助手”新增统一的响应等待超时，可选 1、2、5、10、15 或 30 分钟，默认 5 分钟；反推、优化、翻译、角色替换和自定义改写共用该设置，等待期间仍可随时取消，超时报错会提示调整入口。
- 固定词管理弹窗新增“仅显示启用条目”筛选，可与正负提示词搜索一起使用；关闭筛选后恢复显示全部条目，不改变条目原有的启用状态。

### 🐛 修复

- 修复 Android 智能代理未启用后台运行保护的问题；任务期间显示统一的 AI 任务通知，图像生成与智能代理同时运行时，不会因其中一项结束而提前停止另一项的后台保护。
- 修复智能代理反推聊天图片附件或应用内图片时提示找不到图片路径的问题；桌面端和 Android 均可直接读取所选图片，仍支持本地图片文件，并在图片缺失时明确提示。
- 修复 GitHub 云备份无法直接使用新建空仓库的问题；首次手动推送时自动初始化同步目录，保存连接和读取备份不会写入仓库。
- 修复 GitHub 备份显示推送成功后，拉取时报“备份格式或完整性校验失败”的问题；快照清单、数据对象和 HEAD 现在会完整提交。若旧备份已出现此错误，请从仍保有完整配置的设备重新推送一次，再从其他设备拉取。
- 修复精准参考类型选择弹窗的布局异常，并统一 Emoji 选择器的自适应展示入口。
- 修复保存预设、保存 Vibe、添加词库条目、正则规则、添加词组和 Vibe 详情在大字号、短屏幕及键盘弹出时的内容溢出或操作不可达问题，受限空间下可滚动访问内容与操作。
- 提高 Fluid Saturated、Brutalist、Apple Light 和系统浅色主题中部分次要文字的对比度。
- 修复添加账号、提示词复制、图片拖入操作、Vibe 命名与错误提示、ZIP 元数据导出、水印选色和统计日期等短弹窗被撑得过高、留下大块空白的问题；弹窗现在随内容收缩，长内容仍可滚动查看。
- 修复 Discord 分享弹窗在验证和编辑时占用过多高度的问题，分享编辑区随内容调整高度，并保留固定的底部操作区。
- 修复固定词的正负类型、前后缀、词库写入模式和 Vibe 追加/替换选项在窄屏或大字号下意外变为竖排的问题；选项保持横向排列，空间不足时可横向滚动。
- 修复图片导入提示词、图库搜索、Vibe 分类重命名和数字编辑输入框出现重复背景色块的问题。
- 修复从词库选择固定词时，没有预览图的条目无法显示悬浮文字预览的问题。
- 修复词库条目编辑器底部的提示词辅助操作被正文滚动区域截断的问题，滚动正文或弹出键盘时仍可完整显示。
- 修复右键菜单的鼠标悬浮和键盘焦点高亮被菜单背景遮挡的问题。
- 修复桌面端从托盘退出后窗口仍停留、且清理期间可能再次打开的问题；退出时立即隐藏窗口和托盘，待数据保存与清理完成后结束进程。

## [4.0.1] - 2026-09-05

### 🛠 改进

- 固定词侧栏的复制、编辑和删除改为常驻行内操作，鼠标悬停时会突出显示，触屏和大字号下也会保留足够的点击空间；条目正文不再重复占用一行显示中文译文，翻译仍可在悬停预览中查看，网格卡片也更加紧凑。

### 🐛 修复

- 修复 Fluid Saturated、Material You 和 Apple Light 等主题使用超大卡片圆角时可能裁掉设置分组标题首字的问题，同时让 Hand Drawn 等主题声明的卡片与菜单形状正确生效。
- 修复桌面端词库分类的操作菜单展开后，“重命名”“添加条目”“添加子分类”“移到根级”和“删除”等操作点击无效的问题。
- 修复固定词侧栏条目的复制、编辑和删除菜单在鼠标移出条目后不执行的问题，操作现在会被可靠响应。

## [4.0.0] - 2026-09-05

> [!WARNING]
> **重要提示：** 本次 4.0 更新涉及大量功能与底层调整，发布前的测试覆盖相对有限，仍可能存在尚未发现的问题。建议更新前备份重要配置与数据；如遇异常，请通过“设置 → 关于 → 导出诊断日志”反馈。

### ✨ 新增

- 云同步新增 Google Drive 与 OneDrive，可在 Windows、macOS 和 Android 通过系统浏览器连接账号；与 GitHub、WebDAV 一样，保存连接后仍由用户手动决定何时推送或拉取。
- 云同步新增细粒度内容选择，可分别控制设置、提示词与词库、词库预览图、在线画廊设置与收藏、本地相簿、智能代理提示词与 Skill，以及默认关闭的 Vibe 和精准参考；本地画廊原图、远程图库原图、凭据、缓存和日志始终不会上传。
- 智能代理新增自动创建局部重绘蒙版、扩展画布和装载重绘草稿能力；代理只有在实际查看源图后才能规划蒙版，草稿会先送入重绘面板供用户复核，真正提交生成时才会消耗 Anlas。
- 智能代理新增 Gemini 3.8 Flash 兼容，支持 Google 原生接口、第三方 Gemini 兼容中转与 OpenRouter 路由，能够识别 100 万 Token 上下文、Low / Medium / High 思考等级和工具调用。
- 本地与在线图库、Vibe 和精准参考图片卡片新增“发送到智能体”入口，可从桌面悬浮操作或触屏菜单直接加入对话输入区。
- 提示词工作台新增本地快速汉化预览，支持主提示词、正负面固定词、角色词和词库编辑器中的复杂 NovelAI 权重语法；预览不会改写原提示词，并会结合内置翻译与已安装的 ffdkj 词库，提示词助手则只把本地未命中的标签交给 AI 翻译。
- 精准参考库新增多选与批量修改类型、发送和删除，并支持将所选条目连同图片导出为可校验的 `.naipreciseref` 配置包后再次导入。
- 在线画廊可结构化展示 AI TAG 图片的模型、采样器、CFG、Steps、Seed、尺寸和 LoRA 等生成信息，并为可识别的 NovelAI 图片选择性替换提示词、固定词及生成参数。
- 固定词侧栏新增按分类折叠、全部展开/收起、仅看启用和侧栏收起功能，固定词与词库编辑器也可直接调用提示词助手。
- “关于”设置新增诊断日志导出，可收集最近日志与基础设备信息，并自动隐藏凭据和本地路径，便于排查 Danbooru 等网络问题。
- Android 本地反推支持直接导入包含模型与标签文件的 ZIP 压缩包，并兼容 CL Tagger v2 与 external-data 模型布局。

### 🛠 改进

- 全面重构跨端响应式界面，生成工作台、智能代理、在线与本地画廊、Vibe、精准参考、词库、设置、统计、弹窗和图像编辑器会根据实际可用空间自适应重排；手机触控、横屏、平板、桌面、大字体、软键盘与安全区下均保留完整操作能力。
- 云同步改用可复用未变化内容的精简备份流程，支持并行分块传输、完整性校验、明确的分阶段进度和传输统计；本地相簿仅保存结构与相对路径引用，在线收藏仅保存来源引用，词库预览图会在上传前压缩。
- 云备份统一为简单明文快照，不再使用恢复密钥或 KEY 文件；连接账号与数据传输彻底分离，并补充登录过期、权限、配额、限流、冲突、网络和数据校验等明确错误提示。
- 启动流程延后非关键预加载并限制后台并发，必要数据就绪后即可进入应用；头像、图库缩略图、在线图片和资料库资源改为按需加载，减少首帧等待和页面卡顿。
- Vibe、精准参考、词库与本地图库统一资料库工作区、搜索排序、视图切换、分类侧栏、悬浮预览和卡片操作；桌面支持拖拽分类，触屏提供等价菜单，收藏与分类状态更易识别。
- 本地画廊的相簿树与分类树支持拖到条目前后进行同级排序、拖到中部移入子级、拖到分区标题移回根级，并新增“移到上一级”的触屏入口。
- 智能代理重新整理模型、思考强度、权限、联网和上下文状态，新增可按名称、ID 或服务商搜索的模型选择器，并从智能代理设置直接进入服务商管理。
- 提示词、固定词和角色悬浮预览统一展示完整原文、中文翻译、组成与语义配色，长内容可停留并滚动查看；翻译查询会复用缓存以减少重复等待。
- 图片元数据导入会保存并识别生成时的固定词快照，兼容旧版字段，并在图片未记录固定词时让用户明确选择关闭或保留当前固定词，避免把普通提示词误判为固定词。
- 随机词库简化为两条清晰路径：默认预设始终使用对应模型的 NovelAI 官网词库与算法，自定义预设只使用自身规则；预览区、统计口径和随机按钮同步调整，移除容易混淆的混合模式。
- 全局界面统一使用低对比度分层色面、无边框卡片、紧凑页面标题与工作区顶栏；滑条移除间隔点，设置、统计、生成、画廊和智能代理的信息层级更加一致。
- 移动端补充点击编辑区外收起键盘、生成预览透明背景开关和更紧凑的底部导航、分页、队列、角色与提示词工具栏；不适用于触屏的快捷键设置不再显示。
- 导入图片时，非 SD 系列模型会尽量保留原图的 64 对齐尺寸，仅在超过请求面积上限时收敛到最接近的合法尺寸；编辑器和尺寸提示也会显示实际发送尺寸，避免高分辨率源图被无谓缩小。

### 🐛 修复

- 修复 Google Drive、OneDrive、GitHub 与 WebDAV 在首次连接、授权取消、凭据保存、Android 回调、备份拉取、瞬时上传失败和不支持安全并发写入的服务上的多项问题，并避免坚果云连接探针误报冲突。
- 修复 Gemini 3.7 Flash 首轮请求因不支持的思考等级而失败，以及 Gemini 流式回复报错、工具参数丢失、工具结果无法识别或回复提前结束等兼容问题。
- 修复 Windows 启动时最大化状态、普通窗口尺寸和屏幕边缘位置恢复错误的问题，桌面窗口现在会可靠保存并恢复上次状态。
- 修复图像编辑器画笔圆环跟不上指针、调整画笔尺寸或中键平移后状态卡住，以及拖动外扩画布手柄时同时在画布误落笔的问题。
- 修复相簿右键菜单中的重命名、新建子相簿、移到根级和删除点击无效，以及相簿与分类跨层拖放后顺序不一致的问题。
- 修复主界面中的右键菜单和弹出菜单整体偏移或被错误收拢的问题，覆盖图库、相簿、词库、Vibe、提示词选择器和智能代理资源区等入口。
- 修复在线画廊筛选菜单在移动端不即时刷新、连续工具审批卡状态互相干扰、详情加载提示难以取消，以及纵向图片卡片悬浮操作拥挤的问题。
- 修复固定词和提示词预览中的翻译越界、长内容裁切、滚动失效、悬浮动画冲突、网格文本溢出、权重悬浮崩溃和类别切换时分栏跳动等问题。
- 修复经典布局角色编辑入口消失、位置和顺序错误，以及移动端无法稳定切换当前编辑角色、位置模式入口截断、角色数量徽章被裁切等问题。
- 修复移动端提示词清空按钮、透明底色选单和操作菜单在窄屏下被裁切或溢出的问题。
- 修复透明图片进行原图对比时底层内容串透，以及预览信息栏在窄宽度下挤压 Seed 或占用过多空间的问题。
- 修复更换源图或退出重绘后仍残留上一张图的聚焦重绘状态，聚焦开关与选区现在会随源图一起正确重置。
- 修复 Director Tools 可能在未明确确认时执行消耗操作，现在会先显示工具名称与预计 Anlas 消耗并等待确认。
- 修复主题容器色阶在部分主题下层级不清或偏色的问题，Android、Windows 与 macOS 现在使用一致的中性色面语义。

## [3.2.0] - 2026-08-31

### ✨ 新增

- 新增跨平台水印工作室，可为图片组合文字与 Logo 水印，调整艺术字体、颜色、描边、阴影、透明度、位置和横纵布局，并按横图、竖图与方图分别记忆方案；所有处理均在本地生成副本，不改动原图，还可选择是否保留生成元数据。
- 生成结果、历史记录、本地与在线画廊以及智能代理图片操作统一加入水印入口，支持保存、分享、打开或基于原图重新生成水印副本。
- 本地画廊新增可嵌套的逻辑相簿，支持新建子相簿、拖拽或批量加入图片、重命名、调整层级和移出相簿；相簿只管理引用而不移动或删除原文件，并可随云同步备份与恢复。
- 智能代理输入框新增斜杠命令菜单，可直接引用已启用的 Skill，或新建、重命名、压缩和删除会话；手动压缩会显示压缩前后的 Token 数及不可执行原因。
- 智能代理设置新增自定义上下文窗口，可为第三方中转站或未收录模型补充真实 Token 上限，以正确显示上下文用量并执行压缩。
- 智能代理新增图生图源图工具，可查看、载入、清除源图并调整图生图强度。
- Android“更多”面板新增读取图片元数据入口，可从设备图片提取 NovelAI 提示词与生成参数并选择应用。

### 🛠 改进

- 本地图片详情支持一键复制全部 TAG；本地与在线画廊均可按主提示词、固定词、角色及正负面内容精确选择，法典、AI TAG 等来源会保留各自提供的原始提示词结构。
- 在线画廊详情改用贴近图片的快捷操作轨，并继续在信息区保留主要生成操作；只有标签、没有媒体或完整元数据的条目也可使用生成与加入队列。
- 角色提示词编辑区支持垂直调整高度，提示词助手展开后使用独立区域；工具栏、清空确认和窄屏按钮布局也更加紧凑稳定。
- 第三方 API 站点不再强制要求订阅信息接口；只要兼容图像生成即可登录，无法读取 Anlas 时会显示明确状态，不支持流式生成时会提示关闭流式预览。
- 在线画廊、设置、词库和生成历史会在窗口尺寸、分栏数量或响应式布局变化后保持当前浏览位置。
- 图片右键菜单改进弹出时机、视口边界和超长内容滚动，精准参考库与 Vibe 库不再因打开菜单而整批闪烁。

### 🐛 修复

- 修复 Android 从生成结果或本地画廊保存图片时可能无法写入系统相册的问题，并为系统相册导出失败提供明确反馈。
- 修复 Android“更多”面板底部内容被系统安全区遮挡或无法完整滚动，以及缩略图裁剪、预设编辑等移动端对话框在窄屏下溢出的问题。
- 修复 Windows 最小化期间页面被零尺寸布局销毁重建，恢复窗口后可能丢失页面状态、滚动位置或出现界面异常的问题。
- 修复中文、日文输入法的预编辑文字没有下划线标识，难以区分尚未确认内容的问题。
- 修复退出增强、云端超分或本地超分后，原先选择的尺寸与强度被重置的问题。
- 修复在线画廊复制全部 TAG 在部分来源元数据为空时只得到大括号，以及角色、负面或原始提示词内容复制不完整的问题。
- 修复提示词助手清空交互、角色助手工具栏尺寸、在线画廊后台刷新时序和卡片操作提示残留等界面问题。
- 修复设置中的在线画廊黑名单卡片布局与其他入口不一致的问题。

## [3.1.0] - 2026-08-30

### ✨ 新增

- 生成结果新增原图对比模式，支持图生图、局部重绘、云端超分和本地超分，并提供拖动分隔线、同步缩放与键盘调整。
- 智能代理补全生成图片操作，可引用当前或历史图片执行收藏、词库缩略图、图生图、局部重绘、增强、超分、变化图和 Director Tools。
- 智能代理资源选择器新增统一搜索与本地画廊分页，并在消息中显示已发送资源的缩略图。
- 智能代理新增阅读字号和舒适/紧凑密度设置。
- 智能代理支持编辑最后一条用户消息并重新发送。
- Windows 新增应用自绘标题栏，支持窗口拖动、双击最大化和完整窗口控制。

### 🛠 改进

- 新安装默认使用 NovelAI Diffusion V5 Full，默认 Steps / CFG 调整为 `28 / 4`，已有用户设置保持不变。
- 在线画廊通过预计算瀑布流、滚动速度感知分页和延迟解码提升长列表滚动性能。
- 在线画廊分页按不同来源的真实响应边界定位，避免空页、跨页跳转和过滤结果出现丢失、重复或错序。
- 在线画廊切换来源、刷新和重试时统一使用输入框中的当前内容。
- 在线画廊重启后按已保存的浏览条件重新获取最新内容，不再恢复可能过期的页码和滚动位置。
- 智能代理多角色生成统一使用完整角色快照，并按模型限制处理自动布局与手动坐标。
- 智能代理聊天优化消息对齐、多图布局和滚动跟随，手动阅读历史内容时不再被流式回复抢走位置。
- 生成工作区优化提示词高度、角色折叠面板和侧栏宽度，为窄窗、横屏与大字体保留可用预览空间。
- 提示词助手移至输入框下方工具栏，并补齐移动端自定义和角色替换入口。
- Android 更新弹窗改用窄屏响应式布局，更新日志可独立滚动，操作按钮会保留触屏尺寸并自动分行。
- 预发布版本检查改用分页 Release 数据，能够正确发现最新 Beta。
- 固定词和图片元数据统一使用支持嵌套权重的提示词解析，重复导入时会复用已有条目。
- 设置页与智能代理侧栏改进宽度自适应，宽屏内容保持居中，窄屏入口不再拥挤。
- 智能代理附件缩略图按实际显示尺寸解码，降低长会话的内存占用。

### 🐛 修复

- 修复局部重绘生成透明内容时无法清除原图像素的问题。
- 修复批量生成结果点击后混入无关历史图片的问题。
- 修复编辑用户消息失败后丢失原会话和草稿的问题。
- 修复 Android 主页面一次返回直接退出，以及 predictive back、子页面和覆盖面板返回行为不一致的问题。
- 修复 AI TAG 缩略图长期停留在加载状态，以及滚动后请求无法恢复或失败图片无法重试的问题。
- 修复智能代理浏览在线画廊时可能卡死，且停止按钮无法终止请求的问题。
- 修复智能代理读图类工具重复展示同一图片的问题。
- 修复智能代理意外中断后无法完整恢复当前操作、待发送消息和预分配回复的问题。
- 修复智能代理续跑、结束回复和切换会话时可能发生的状态竞态。
- 修复智能代理角色布局、确认执行、队列和重试之间可能使用不同角色状态的问题。
- 修复 V5 角色上限与实际请求参数不一致的问题。
- 修复固定词重复添加，以及嵌套括号或权重提示词被错误拆分的问题。
- 修复浅色主题下角色折叠面板出现白底白字的问题。
- 修复桌面生成页侧栏和大字体控件在窄宽度下溢出或挤占主预览区域的问题。
- 修复启动界面未使用 Windows 标题栏，以及窗口最小化时布局溢出或标题栏按钮状态不同步的问题。

## [3.0.0] - 2026-08-30

> [!WARNING]
> **此版本可能存在较多 Bug，且仍有大量功能未经测试。** 本次新增了 Android 端，并进行了大量跨平台适配、架构重构和交互调整；建议仅用于尝鲜，升级前请备份重要数据，遇到问题请及时反馈。

### ✨ 新增

- 新增 Android 7.0+ 客户端，完整提供生成、画廊、词库、队列与设置，并适配手机横竖屏、平板和大屏；接入系统文件选择、导出、相册保存、分享、后台生成通知及 APK 更新。
- 新增全局智能代理：可在桌面侧栏和移动端抽屉中持续多轮对话，查看与调整 Prompt、角色、生成参数、参考图、词库、队列和画廊内容；支持会话历史、资源引用、工具审批、Skills、自定义系统提示词及多模型思考参数。
- 新增智能代理联网工具，可按需使用 SearXNG、匿名 Exa MCP 或 Exa API 搜索并读取公开网页；联网默认关闭，权限、文件访问和所有 Anlas 消耗操作均提供明确控制与单独确认。
- 新增跨端同步与备份，可通过 GitHub 或 WebDAV 手动推送、拉取所选数据，支持完整性校验快照、历史与恢复、逐项冲突处理、智能代理提示词和 Skill 备份；凭据和 NovelAI Token 不进入备份，能力不足的 WebDAV 服务仅提供手动备份。
- 在线画廊新增法典图鉴（NovelAI QuickTagCloud），支持固定公开法典、分类与版本浏览，多图及纯文本词条、完整贡献者署名、内容分级、本地收藏、最近浏览，以及 Prompt 复制、生成和加入队列。
- 在线画廊新增跨来源本地收藏、统一黑名单和最多六 Tag 组合搜索；统一多来源详情、提示词与角色投影、媒体下载和标签操作。
- 随机词库完整还原 NovelAI 官网数据，并按当前模型自动选择 Legacy Anime、Furry V3 或 Character Prompts；自定义模式改用完整离线标签 catalog，混合模式会同时使用两套来源。
- 生成页新增 V5 噪声调度、Variety+、自动文本、构图参考线与流式预览开关；角色支持独立负面提示词语法，多角色导入、反推和参考面板可独立折叠并记忆状态。
- 支持从 WebP 读取生成元数据，并可从图片中选择正面、负面或角色提示词直接创建词库条目；自动补全支持 `artist:` 前缀筛选画师。
- 未登录时现在可直接进入应用使用本地功能，只有生成、Vibe 编码、云端超分等付费操作才会要求登录。

### 🛠 改进

- 全面适配 Windows、macOS 与 Android 的响应式导航、生成工作区、提示词编辑、画廊、词库、Vibe 库、统计和设置页面，并统一为更简洁的静谧层叠视觉体系。
- 重构移动端生成交互：提供专用工作区、全屏提示词编辑器、角色管理面板、纵向快捷手势、软键盘与系统返回处理，在窄屏下保留与桌面端一致的业务能力。
- 优化在线画廊性能：图片按可见优先级加载，限制预取和重试并真实取消过期请求；分页使用无阻塞占位卡片，大型列表滚动、随机加载和详情切换更稳定。
- 优化本地画廊启动与缩略图：改为进入画廊时按需扫描，按显示尺寸动态解码并限制内存缓存，同时安全迁移旧缩略图，降低空载 CPU/GPU 占用。
- 重构设置页分组和响应式布局，统一账号、外观、生成、智能代理、联网、存储与同步入口，改善卡片层级、对比度和紧凑尺寸可读性。
- 优化智能代理聊天界面：统一桌面侧栏和移动端消息时间线、工具状态、图片预览、会话切换、输入队列、滚动跟随与 SafeArea，并支持编辑后重新发送消息。
- 优化图片生成网络调度：画廊后台加载会主动避让生成、增强和余额刷新等关键请求，减少慢代理环境下互相阻塞。
- Windows/macOS/Android Release 统一从不可变 tag commit 构建；Android APK 强制正式签名并校验包名、版本和证书，所有安装包继续附带大小与 SHA-256 校验信息。

### 🐛 修复

- 修复生成流式预览透明区域可能残留上一张图片，以及队列任务并发完成时可能跳过或重复推进的问题。
- 修复未登录点击生成时没有直接进入登录页，以及部分付费入口在上传或进入运行状态后才提示登录的问题。
- 修复本地画廊大型索引扫描卡顿、标签搜索遗漏，以及启动后即使未打开画廊也持续扫描和占用资源的问题。
- 修复在线画廊图片预加载取消后仍继续工作、详情页使用错误预取上下文、AI TAG CDN 图片请求失败及 Gelbooru 公共画廊分页异常。
- 修复 ffdkj 中文词典下载返回 403、AI 翻译缓存重启后未立即恢复，以及动态翻译悬浮提示显示不稳定的问题。
- 修复固定词悬浮预览可能越出窗口，并恢复可选的鼠标滚轮调权功能；角色自定义助手不再因编辑器焦点切换意外中断。
- 修复桌面侧边栏展开收起时反复重排导致卡顿、折叠状态提示位置错误，以及历史图片滚动停止后悬停状态不刷新的问题。
- 修复智能代理中断后无法继续、会话与权限状态切换错乱、工具调用后出现空白消息，以及部分模型的思考参数和推理内容无法连续传递的问题。
- 修复云端同步连接保存后意外开始同步、应用云端数据后运行时配置未刷新，以及 WebDAV 目录探测和网络错误提示不清晰的问题。

## [2.0.0] - 2026-08-23

### ✨ 新增

- 新增 Discord 社区分享：可从生成结果和本地画廊直接发送图片，支持浏览器成员验证、多频道选择、附言、提示词类别编辑与发送偏好记忆；默认移除图片元数据，并完整提示未加入服务器、会话失效、限流及部分频道失败等状态。
- 新增安全复制正面提示词：可按主体、角色、质量词和固定词选择复制范围，默认排除可能包含私密内容的固定词与自动质量词；旧图片也会结合当前固定词库恢复分类。
- 新增本地画廊批量标签编辑，支持中英文逗号和换行输入、忽略大小写去重、添加与移除冲突处理，并可让批量操作转到后台继续。
- 新增 ZIP 导出元数据选项：可保留原图，或仅对压缩包副本移除 PNG 文本、EXIF 与 NovelAI 隐写元数据；导出改为后台流式处理，提供逐图进度和部分失败明细。
- 新增 NovelAI 多角色提示词粘贴导入：识别单竖线分段格式并自动拆分主体与最多 6 个角色提示词，同时兼容 Discord 代码块内容。

### 🛠 改进

- 优化图片详情页：正负提示词统一分类展示并标记角色词、固定词，缩略图栏支持鼠标滚轮横向浏览、点击准确跳转，同时移除底部冗余元数据以扩大图片空间。
- 优化本地画廊、Vibe 库与词库卡片的悬浮效果和操作按钮，统一选中工具栏布局；横图与竖图会自动调整按钮方向，复制图片时显示进行状态并避免误触打开详情。
- 优化本地与在线画廊悬浮预览：补充读取完整生成元数据，压缩信息布局，并消除内容较少时的空白区域。
- 优化更新体验：每次启动都会检查新版本，同时继续遵守“稍后提醒”和失败冷却；更新弹窗扩大日志区域、改善明暗主题可读性、提供 Release 页面入口，并隐藏内部构建号。

### 🐛 修复

- 修复 AI TAG 搜索刷新时丢失当前关键词，以及在线画廊加载失败后被滚动加载反复触发请求的问题；分页失败现在可在原位置明确重试。
- 修复图片详情查看器在分页追加后可能复用错误缩放状态、缩略图点击跳转错位，以及无双击回调的画廊卡片仍意外打开详情的问题。
- 修复带 EXIF 方向信息的图片在复制、导出净化副本或分享到 Discord 后发生旋转的问题。
- 修复繁体中文标签搜索所需的 OpenCC 映射未随安装包正确打包，导致部分转换无法使用的问题。
- 修复更新说明包含 GitHub Alert 时可能导致更新弹窗崩溃，以及部分自定义主题下正文和链接对比度不足的问题。

## [1.9.0] - 2026-08-23

### 🚨 必看：`v1.8.0` 用户必须手动升级

> [!IMPORTANT]
> **如果当前使用的是 `v1.8.0`，请勿使用应用内更新，请直接前往 [v1.9.0 Release 页面](https://github.com/Aaalice233/Aaalice_NAI_Launcher/releases/tag/v1.9.0) 手动下载安装。**
>
> `v1.8.0` 自带的更新启动逻辑存在缺陷：应用内下载完成并退出后，可能无法启动安装程序。这只是一次性手动升级；`v1.8.1` 及之后的版本可以继续使用应用内一键更新。

### ✨ 新增

- 新增完整繁体中文界面，可在“设置 → 外观 → 语言”中直接切换；繁体中文标签输入会在本地转换后继续检索可选的简中汉化词库，不会上传输入内容。
- 在线画廊黑名单新增本地与 Danbooru 云端来源选择：两份列表保持独立，可直接编辑当前来源，并支持手动拉取云端或在确认后用本地列表完整覆盖云端；未登录时自动使用本地列表。
- 本地画廊与 Vibe 词库新增延迟悬浮预览：可查看高清大图以及分辨率、文件大小、日期、模型、种子、步数，或 Vibe 强度、信息提取率、使用次数和标签；预览会根据图片比例与窗口边界自动调整。
- 在线画廊会保存当前来源、模式、搜索与筛选条件、分页位置和滚动位置；重新启动应用后会重新获取远端内容并回到上次浏览位置，搜索、热门、收藏和随机模式均可恢复。
- 重做生成队列入口与执行体验：移除遮挡内容的悬浮球，桌面侧栏与移动端底栏提供固定入口并显示待执行数量和运行状态；队列面板可加入当前提示词并直接开始、暂停或继续队列，从任意页面启动也能自动连续执行。

### 🛠 改进

- 优化在线画廊滚动加载：每页获取数量提高到 60，并根据窗口高度和列数提前翻页、构建与预取下一段缩略图，减少快速滚动时的空白和等待；AI TAG 不再重复预取与原图相同的预览资源。
- 队列任务与执行设置会在关闭应用后完整保留；暂停会等待已经提交的当前任务完成后生效，重新开始一轮队列时会重置本轮统计。
- 从在线画廊、详情页或拖入图片加入队列时会明确显示当前任务数；批量加入遇到容量上限时会分别报告已加入与未加入数量，队列为空时也可直接通过图片拖放创建任务。
- 生成与取消共用稳定的按钮尺寸和主按钮样式，生成中平滑切换为醒目的取消状态，避免控件跳动；桌面 Toast 的图标、文字与关闭按钮也改为垂直居中。
- Opus 免费配额回充超过 100% 时，百分比与预计可生成张数会保留真实额度，仅进度条封顶显示。
- 展开主侧栏时会显示当前应用版本，侧栏收起动画和窄宽度布局也更稳定。

### 🐛 修复

- 修复在线画廊连续跳过多页被过滤的空结果后，继续加载可能误用数字页码而不是来源游标、导致后续内容无法接上的问题。
- 修复部分情况下重启应用后生成队列任务或执行设置无法从 Hive 正确恢复的问题。
- 修复在线画廊黑名单此前自动合并本地与云端列表并回写云端，可能意外改变用户 Danbooru 黑名单的问题；现在只有明确执行推送操作才会覆盖云端。

## [1.8.1] - 2026-08-23

### 🚨 必看：`v1.8.0` 用户必须手动升级

> [!IMPORTANT]
> **如果当前使用的是 `v1.8.0`，请勿使用应用内更新，请直接前往 [v1.8.1 Release 页面](https://github.com/Aaalice233/Aaalice_NAI_Launcher/releases/tag/v1.8.1) 手动下载安装。**
>
> `v1.8.0` 自带的更新启动逻辑存在缺陷：应用内下载完成并退出后，可能无法启动安装程序。这只是一次性手动升级；该问题已在 `v1.8.1` 修复，之后的版本可以继续使用应用内一键更新。

### ✨ 新增

- 本地相关标签共现数据改为独立可选数据包：进入主页后可在后台自动下载，支持暂停、断点续传、重试、修复、更新与删除；下载或安装失败时仍可继续使用基础补全和 Danbooru 在线相关标签。
- “设置 → 数据源与缓存”新增本地相关标签数据管理，可查看下载进度、版本、关系数量和磁盘占用，也可关闭自动下载或删除数据后避免下次自动安装。

### 🛠 改进

- 大幅缩小应用安装包：不再内置约 306.6 MB 的旧共现数据库及构建用 CSV，只保留完整的 Danbooru/e621 基础标签与别名目录；独立数据包下载约 30.3 MiB，安装后约占用 78.7 MiB。
- 共现数据包与应用启动完全解耦，并增加来源固定、大小与 SHA256 校验、SQLite 完整性检查和原子替换；旧版内置共现数据会在确认来源后安全清理，不会误删用户文件。
- 应用更新包下载支持断点续传，并在复用或安装前同时校验文件大小与 SHA256；独立数据包 Release 不再被识别为应用更新。

### 🐛 修复

- 修复应用内更新下载完成并确认重启后，应用虽然退出但安装程序没有启动的问题；现在会先确认更新进程成功启动，启动失败时保留应用并明确报错。
- 修复部分代理或共享网络下 GitHub API 限流导致检查更新失败的问题；稳定版与预发布版检查均不再消耗匿名 API 配额，失败提示也不会再显示内部异常或撑破弹窗。
- 修复在线画廊向上滚动时，图片详情加载会改变卡片高度并带动周围内容跳动的问题；卡片进入瀑布流后会保持稳定布局。
- 修复应用启动后没有真正执行自动更新检测、只能手动点击“检查更新”才出现提示的问题；现在进入主界面后会自动检查，并在发现新版本时显示全局更新提示。
- 修复 `1080×1920` 等不符合 NovelAI 请求要求的分辨率直接提交后才报错的问题；尺寸选择区和生成错误现在会说明 64 倍数、单边及总像素限制，并给出最接近的可用尺寸，Krita 生成请求也会在发送前完成相同校验。

## [1.8.0] - 2026-08-22

### ✨ 新增

- 在线画廊新增随机模式：可按当前来源与筛选条件持续抽取未看过的作品，一键重抽或重新开始；随机结果独立保存浏览位置，不会打乱普通浏览列表。
- AI TAG 来源新增“仅画师串”模式：可在搜索、热门与随机模式中精确筛出正向 Prompt 含 `artist:` 标签的作品；多图作品仍只显示一张作品卡片，提示词与画师串相同的重复结果会自动合并，并可从卡片或详情一键复制保留原顺序与权重的画师串。
- 在线画廊新增提示词类别选择，可决定复制、发送到主提示词或加入队列时是否包含通用、角色、版权、画师和元数据标签，并会记住选择。
- 在线画廊新增输出过滤：可集中维护不希望出现在输出 Prompt 中的标签，也可在标签右键菜单中直接加入；图片仍正常显示，仅在复制、发送和加入队列时精确剔除。
- 生成结果图下方新增信息条，与 NovelAI 网页端一致：显示图片分辨率与种子，点击种子即可填回生成参数；预览区太窄时会自动收起分辨率。
- 信息条上的棋盘格按钮可以设置透明图片的显示底色，提供跟随主题、浅色、深色三档棋盘格，以及无底色、六种纯色和自定义取色；透明背景生成的图片不再只能透出面板底色。
- 生成设置新增透明图像 Alpha 模式，可在 Straight 与 Premultiplied 之间切换；所选模式会用于支持透明输出的请求并写入图片元数据。
- 词库搜索框接入标签自动补全；固定词侧栏和管理界面中的词库关联项支持悬浮查看预览图、标签与最后使用时间。

### 🛠 改进

- 重构在线画廊的图片加载、详情缓存与网络重试流程：优先加载可见内容并渐进显示清晰图，快速滚动、连续翻页和多图详情浏览更流畅，临时网络错误会按来源规则自动重试。
- 优化在线画廊工具栏与悬浮预览：搜索、热门选项和操作按钮会随窗口宽度自动分行；登录及认证状态改为紧凑头像徽标，悬浮大图会根据图片比例和可用空间自适应布局。
- 重做固定词编辑弹窗和前后缀选择器：宽屏采用编辑区与设置区双栏布局，窄屏自动切换为纵向布局；固定词管理项可点击名称或内容直接启用/禁用，关联词库条目的恢复与预览也更稳定。
- 词库预览图会按卡片实际比例完整铺满；在线画廊标签输入同时支持空格、逗号等分隔方式，标签输入框与添加按钮保持对齐。
- 优化应用启动顺序：更早显示启动界面，并行初始化必要服务、延后非关键清理和预热；初始化失败会明确显示原因并允许直接重试，不再只能退出后重新启动。
- README 改为面向使用者的精简说明，版本徽标会自动跟随最新 Release，开发与发布文档统一移至仓库维护指南。

### 🐛 修复

- 修复 Windows 安装版和便携版无法读取部分 Release 更新清单、检查到更新后仍会打开 GitHub 下载页的问题；现在可继续在应用内下载并安装。
- 修复应用内更新日志先显示平台下载和文件校验说明、实际更新内容需要滚动查找的问题；现在会直接展示 Release 的“更新内容”正文。
- 修复关闭自动补全后部分入口状态不同步或弹窗仍停留，以及固定词、词库、队列等子输入框获得焦点后联想菜单不显示的问题。
- 修复从词库添加固定词时仍列出已经关联的条目，可能造成重复添加的问题。
- 修复角色定位画布被历史预览图比例带偏，导致定位坐标与下一次生成尺寸不一致的问题。
- 修复元数据导入界面无法识别中文字段名中的全角冒号，导致 CFG 强度等标签残留多余冒号的问题。

### 👥 贡献者

- 感谢 [@rabiarabbit](https://github.com/rabiarabbit) 通过 [PR #81](https://github.com/Aaalice233/Aaalice_NAI_Launcher/pull/81) 和 [PR #82](https://github.com/Aaalice233/Aaalice_NAI_Launcher/pull/82) 贡献本版本的生成预览信息条、透明图像设置与角色定位修复。

## [1.7.2] - 2026-08-21

### 🐛 修复

- 修复登录 Danbooru 后收藏夹始终为空的问题；收藏内容现在会按收藏时间正确加载，登录成功后当前收藏夹也会自动刷新。

## [1.7.1] - 2026-08-21

### 🛠 改进

- V5 透明背景开关移至正向提示词框左下角，与 NovelAI 网页端布局保持一致；负面提示词与不支持该功能的模型不会显示此开关。

### 🐛 修复

- 修复在线画廊在分级、多标签或黑名单过滤后结果不足一屏时无法继续翻页的问题；现在会自动补充后续页面直至瀑布流可滚动或数据加载完毕。
- 修复 Danbooru 画廊搜索、排行榜和详情请求未携带当前登录凭据的问题，账号有权访问的受限内容现在可以正常加载。
- 修复中文或尚未输入完整的标签通过 `Ctrl/Command + 点击` 查询时没有补全结果的问题；完整中文标签会先映射到英文规范标签再查询相关项，未完整标签会显示与键入时一致的普通补全候选。

### 👥 贡献者

- 感谢 [@rabiarabbit](https://github.com/rabiarabbit) 通过 [PR #73](https://github.com/Aaalice233/Aaalice_NAI_Launcher/pull/73) 和 [PR #74](https://github.com/Aaalice233/Aaalice_NAI_Launcher/pull/74) 贡献本版本的界面改进与补全修复。

## [1.7.0] - 2026-08-21

### ✨ 新增

- 支持 NovelAI Diffusion V5 正式版模型（Curated / Full）：模型列表直接可选，包含 Qwen 分词器与按模型的 Token 上限（Curated 703 / Full 1471）、最多 32 个角色与自由定位、透明背景开关、增强 Max 档，以及 V5 专属的质量词与负面预设文案。
- V5 局部重绘接入官方权重：Full 使用独立的 inpainting 模型，Curated 按官网行为使用 V4.5 Curated Inpainting。
- Anlas 预估对齐 V5 正式计价：基础价为通用公式的 1.5 倍；Opus 免费额度改为随时间回充的配额池，配额透支后预估会按正常价显示。
- 图像放大接入 V5 换代后的 `/ai/upscale` 接口，旧服务端自动回退原有格式；计费类错误不会重试，避免重复扣费。

### 🛠 改进

- V5 下自动隐藏官方不支持的选项：噪声调度固定为 karras、Variety+ 不再显示；模型切换会跟随 V5 的默认 CFG 7。
- Opus 账号在 V5 下会在余额旁显示免费生成配额徽章（剩余百分比、估算张数与自动恢复说明），配额透支时转为警示态。
- 移除测试期的"显示测试站模型"设置与 custom 测试条目；测试期保存的模型选择与图片元数据会自动归一到 V5 Curated。
- 导入 V5 正式版生成的图片可正确识别模型（含 Full 指纹），启动器保存的 V5 图片也能被官网识别。
- 质量词预设支持 V5 的官方 Light 档（"very aesthetic, amazing quality, no text"），与 Standard 档在质量词菜单中切换。
- 生成请求补发官网的质量/负面预设数字提示（tag_hint_qt / tag_hint_uc_preset），自定义预设不再误报为 None；导入 V5 图片时会按提示恢复 Standard / Light 档并正确分离质量词与透明背景词。
- V4 及以后模型的采样器列表不再提供官网已移除的 DDIM，存量选择会自动按官网规则改发 Euler Ancestral（此前 V5 下会把 DDIM 原样发出）。
- 透明背景开关纳入元数据导入：导入 V5 透明背景图片可选择恢复该开关。
- 角色数量按官方上限约束（V5 为 32、V4/V4.5 为 6），达到上限后添加入口禁用并提示。
- Opus 配额徽章的估算张数按实际计费尺寸折算（增强 Max 档使用放大后的目标面积）；图像编辑器内的费用预估同样考虑配额透支。
- 词库导航移除“开发中”标记，入口状态与实际可用功能保持一致。

### 🐛 修复

- 修复首次安装仍会执行运行进程检查，检查失败时误报启动器正在运行并阻止安装的问题。
- 修复增删换行、逗号、标签边界空格等结构编辑会误触发标签补全，以及相关标签加载时弹窗可能被语法高亮刷新意外关闭的问题。
- 修复高 DPI 缩放或窄侧栏下 Vibe 卡片操作按钮超出点击区域的问题，并放大悬浮预览、根据窗口空间自动调整位置和尺寸。

### 👥 贡献者

- 感谢 [@rabiarabbit](https://github.com/rabiarabbit) 通过 [PR #72](https://github.com/Aaalice233/Aaalice_NAI_Launcher/pull/72) 贡献本版本的 NovelAI V5 支持、改进与修复。

## [1.6.0] - 2026-08-20

### ✨ 新增

- ComfyUI 超分新增原生 SeedVR2 工作流：可探测服务器节点和模型能力，自动选择原生或兼容后端，并提供是否向结果写入 SeedVR2 元数据的设置。
- NovelAI V3 模型支持直接使用原始图片进行 Vibe Transfer，无需先生成 V4 编码。
- 补全设置新增“点击标签时显示补全”：可选择点击已有标签时打开普通补全菜单；关闭时仍仅在文字变化后显示，`Ctrl/Command + 点击` 的相关标签推荐不受影响。

### 🛠 改进

- 生成参数改为按模型能力统一处理：模型切换时会跟随默认 CFG 与步数，并同步提示词结构、Token 统计、角色定位、Vibe 和 Precise Reference 可用状态。
- 增强生成对齐 NovelAI 网页端：使用固定强度档位，根据原图尺寸提供有效倍率，并自动补充相应的增强提示词。
- Vibe 库在编辑、批量标记编码模型和重新保存时会保留原图、缩略图、混合 Bundle 及其他模型编码；编码状态和预估费用现在会跟随当前模型显示。
- 导入 Vibe 原图时不再立即打断操作并按默认参数编码；图片会先加入列表，用户调整信息提取参数后可主动编码，生成时也会按最终参数自动编码。
- 选择普通标签补全结果后会快速显示首批共现推荐并在后台补齐完整结果；继续选择共现结果时会围绕新标签连续推荐，固定后则始终围绕固定标签推荐。
- 生成历史继续保持跨批次联动浏览，Windows 使用 `Ctrl`、macOS 使用 `Command` 进行多选；调整窗口宽度时不再重建并丢失右侧面板状态。

### 🐛 修复

- 修复 V4 提示词中的质量词可能被追加到 `text:` 文字渲染段，或在混合提示词中落到错误分段的问题。
- 修复符合 Opus 免费条件的生成在启用 Vibe 或 Precise Reference 后把基础生图费用重复计入的问题；现在只显示真实的参考功能附加费。
- 修复已编码 Vibe 可能被重复编码、库文件编辑后丢失来源数据，以及导入元数据时模型参数没有完整保留的问题。
- 修复自动格式化提示词会删除换行、切回窗口后光标跳到末尾，以及应用图片元数据后重复弹出成功确认的问题。
- 修复增删换行、逗号、标签边界空格等结构编辑会误触发标签补全的问题；只有当前标签文字发生变化才自动查询，菜单打开时 `Enter` 保持确认，`Shift + Enter` 强制换行。
- 修复相关标签仍在加载时可能被语法高亮刷新意外关闭的问题；无结果时菜单会保留并明确显示空状态。
- 修复高 DPI 缩放或窄侧栏下 Vibe 卡片操作按钮超出点击区域的问题，并放大悬浮预览、根据窗口空间自动调整位置和尺寸。
- 修复 PNG 中的 Unicode 文本元数据无法可靠保留的问题；重建元数据时会继续保留原始图像像素。

### 👥 贡献者

- 感谢 [@rabiarabbit](https://github.com/rabiarabbit) 通过 [PR #69](https://github.com/Aaalice233/Aaalice_NAI_Launcher/pull/69) 贡献本版本的新增功能、改进与修复。

## [1.5.3] - 2026-08-19

### 🛠 改进

- Opus 免费额度现在同样适用于符合条件的 Image2Image 和局部重绘，符合分辨率、步数和参考图条件时不再误报基础 Anlas 消耗。
- Windows 安装器只关闭当前安装目录中的启动器进程，不再误关其他目录中同名的程序；无法确认进程路径时会停止安装，避免覆盖仍被占用的文件。

### 🐛 修复

- 修复升级基础标签数据库时可能连同本地图库索引一起删除，导致图库数据需要重新扫描的问题。
- 修复多个提示词输入框共用翻译任务，查询或关闭其中一个补全窗口会取消另一个输入框正在进行的 LLM 翻译的问题。
- 修复在线画廊快速切换来源时旧请求可能覆盖新结果，以及整页内容被过滤后页码没有同步前进、继续加载可能重复内容的问题。
- 修复在更新包下载完成并写入待安装状态的短暂阶段无法取消更新，取消后仍可能进入安装流程的问题。

### 👥 贡献者

- 感谢 [@rabiarabbit](https://github.com/rabiarabbit) 通过 [PR #67](https://github.com/Aaalice233/Aaalice_NAI_Launcher/pull/67) 贡献本版本的改进与修复。

## [1.5.2] - 2026-08-17

### 🛠 改进

- 更新提示改为应用内持久通知：登录前后都能看到新版本，设置入口与导航栏会保留更新标记；支持“4 小时后提醒”，已下载更新在重启应用后仍可继续安装。更新日志现可完整渲染 GitHub Flavored Markdown，包括标题、嵌套与任务列表、表格、引用、代码块、链接和图片。
- Windows 更新下载支持真正的断点续传、包大小与 SHA256 双重校验；安装过程会记录结果和日志，失败后自动重新打开旧版本。便携版改用同卷暂存、目录切换和备份回滚，并保留程序目录中的用户文件。
- Windows 手动安装包会主动检测仍在运行或隐藏到托盘的旧版本，确认后关闭旧进程再继续；发布流程支持对主程序与安装器执行 Authenticode 签名及签名验证。
- Release 页面按操作系统展示带平台图标的 Setup / Portable 直达下载按钮，下载入口更直观。

### 🐛 修复

- 修复启动检查发现新版本后因弹窗使用了错误的界面上下文而没有任何应用内提示的问题。
- 修复更新检查在请求 GitHub 之前就记录“已检查”，导致临时断网后 24 小时都不再重试的问题；失败检查现在会在 30 分钟后重试，只有成功响应才进入常规检查间隔。
- 修复直接退出安装可能遗留数据库文件锁、托盘图标或未刷新的日志，以及安装器失败后无退出码、无日志且不会恢复旧版本的问题。
- 修复输入框仅获得焦点、移动光标或关闭补全窗口后仍可能自动弹出的问题；现在只有实际文本编辑才会触发普通补全，关闭窗口也会取消延迟结果，相关标签仅通过快捷键或 `Ctrl/⌘` 点击显式打开。

## [1.5.1] - 2026-08-16

### 🛠 改进

- Anlas 消耗预估同步 NovelAI 当前计费规则：覆盖 V3/V4 生图与重绘强度、Vibe 编码和超额数量、Precise Reference 以及云端超分的分档价格；产生扣费的操作完成后会自动刷新余额。

### 🐛 修复

- 修复输入 `<` 时词库联想被普通标签补全吞掉的问题；现在会立即只显示词库候选，并按收藏与使用频率排序，选中后完整插入 `<词库名>, `。
- 修复补全选中后多标签搜索条件可能被截断、全局显示与插入设置失效、中文输入法组词期间提前查询，以及长按方向键和 `Esc` 操作不完整的问题。
- 修复多行提示词补全窗口未贴近光标行、启动或切换页面时偶发红屏布局断言，以及滚动条遮挡来源标记的问题。

## [1.5.0] - 2026-08-09

### ✨ 新增

- Windows 便携版支持应用内一键更新：直接下载更新包并自动覆盖安装、重启应用，无需再去 Release 页面手动下载替换。
- 更新下载支持实时显示进度、已下载大小与下载速度，可随时取消下载。
- 提示词补全升级为统一的离线优先系统：完整内置 LoRA Manager 使用的 Danbooru/e621 合并词库，包含超过 22 万条主标签与 7 万余条别名；支持可选中文词库、Danbooru 在线新标签补充和 Prompt Assistant AI 缺失汉化。
- 新增相关标签补全：按 `Ctrl/⌘+Shift+Space` 查询光标前标签，合并本地共现关系与 Danbooru 在线关系并显示共现次数/Jaccard 强度；支持固定来源标签后连续插入多个相关词。
- 设置新增补全结果数量、别名/汉化显示、自动逗号和下划线转空格选项；可查看基础词库与共现库状态，按需安装或更新中文词库，并独立清理 Danbooru 在线缓存和 AI 翻译缓存。
- 在线画廊新增 AI TAG：支持作品信息与 Prompt 组合搜索、动态时间范围、实时月榜、历史月榜和旧月份归档；多图作品可逐张查看、复用元数据或整组下载。
- Safebooru 在线画廊新增指定日期的日榜、周榜和月榜，并可与 Danbooru、AI TAG 排行榜独立切换。
- 桌面主导航栏支持展开和收起：展开后直接显示页面名称与当前账号，选择会自动保存，下次启动保持原布局。
- 词库预览图新增 JPG、WebP、GIF、BMP、TIFF、TGA、ICO 等常见格式导入，并自动转换为稳定可显示的预览图。

### 🛠 改进

- 更新流程重做：下载完成后经 SHA256 校验才提示“安装并重启”；重复安装复用已下载的更新包，不重复下载；下载/安装失败时给出明确错误，可直接重试或改用浏览器下载。
- 生成、队列编辑、词库等主要提示词输入入口统一使用同一套补全、排序和插入逻辑；本地候选即时出现，在线与 AI 结果后台原位补充，不再因慢请求打断键盘选择。
- 补全候选默认显示全部匹配项，列表仅构建当前可见行并缓存邻近区域；数千条结果也能平滑滚动，单字符输入仍保留轻量保护以避免无意义地扫描大半词库。
- 补全菜单新增查询词、结果数量和键盘快捷键顶栏，以及基础词库、中文词库、在线补充和 AI 翻译实时状态底栏；支持一键关闭和直达数据设置，并会随可用空间自动精简布局。
- 基础标签数据库安装增加 SHA256、schema、SQLite header 与 `quick_check` 校验，更新失败时保留上一版可用数据库。
- 在线画廊会按当前来源只显示真实支持的搜索、筛选、账号与排行榜选项；切换来源会保留各自结果和滚动位置，加载下一页失败可原位重试，宽屏工具栏也会更充分利用横向空间。

### 🐛 修复

- 修复 Danbooru 在线补全被本地结果上限截断、实际只显示基础词库候选的问题；在线独有的新标签现在会完整分页追加且不打乱本地列表，仍可在设置中按需限制结果数量。
- 修复本地共现查询只读取 CSV 关系一侧，导致大量标签（如 `solo`）缺失大部分相关词的问题；现在会双向读取每一对无序标签关系。
- 修复点击安装更新后旧进程未及时退出导致安装器报错的问题：更新改为由独立辅助脚本等待应用完全退出后再执行安装，彻底消除时序竞争。
- 修复在线画廊热门榜翻页后反复显示第一页图片的问题；现在页码会正确前进，并通过来源作品 ID 和稳定渲染键双重去重，重复页会直接结束加载。

### ⚠️ 注意

- `1.4.0` 及更早的 Windows 便携版尚不具备自动覆盖能力，升级到 `1.5.0` 时仍需从 Release 页面手动下载并替换一次；安装 `1.5.0` 后，后续版本即可直接在应用内更新。

## [1.4.0] - 2026-08-07

### ✨ 新增

- 新增“精准参考库”：把常用参考图保存进独立图库反复使用，支持按类型筛选、批量并发导入、逐条保存当前参考图，并可一键发送到生成页作为精准参考。
- macOS 新增系统托盘：关闭窗口时隐藏到菜单栏而不退出，点击 Dock 或菜单栏图标可恢复窗口，菜单栏菜单提供“显示 / 退出”。
- 提示词设置新增“复制时展开词库”开关：开启后从提示词框复制或剪切时，`<词库名>` 会自动替换成词库里的实际提示词，方便直接粘贴到其他工具；未开启时仍复制原始写法。
- 提示词设置新增“正则替换规则”面板：可自定义多条“匹配 → 替换”规则，输入框失焦时按顺序改写提示词，用来统一 tag 写法、批量换词或清理粘贴来的杂质。规则支持单独启用/停用、拖拽排序、区分大小写，替换内容里可用 `$1`、`$2` 引用捕获组；写错的正则会在列表里标记为无效并自动跳过，面板内还能贴一段提示词实时预览替换结果。

### 🛠 改进

- 生成页工具栏精简，生成按钮居中显示。
- 角色提示词解除最多 6 个角色的限制，角色卡片操作入口统一。
- 浅色主题下文字与控件对比度整体提升，右键菜单和文本选择工具栏跟随主题与用户自定义字体。
- Vibe 列表缩略图改为按需加载，大型 Vibe 库打开更快、内存占用更低。
- 网络错误提示跟随界面语言（中/英/日），不再固定显示中文。

### 🐛 修复

- 修复标签自动补全弹层可能超出屏幕、滚轮滚动到首尾后回绕的问题。
- 修复 ComfyUI SeedVR2 工作流未按最长边缩放导致的输出尺寸偏差。

## [1.3.0] - 2026-08-06

### ✨ 新增

- 生成图片按日期分类保存：保存到图库时自动按 `yyyy-MM-dd` 建子文件夹，文件名带时间和实际 seed（如 `14-30-45-2903847162.png`），与 NovelAI 网页端结构一致；日期子目录在图库中自动成为文件夹分类。

### 🐛 修复

- 修复随机配置页「新增类别」按钮点击无反应的问题，现在点击后输入名称即可创建类别。
- 修复抽卡模式批量生成时，第二张起丢失固定词和质量词的问题。

## [1.2.0] - 2026-08-03

### ✨ 新增

- 在线画廊支持配置 Gelbooru User ID 与 API Key，认证后通过 DAPI 加速搜索，并可浏览 Gelbooru 网站的只读收藏。
- 角色提示词支持在生成预览中拖动连续位置锚点，并可在 AI 选择与自定义位置之间切换。
- 保护模式新增生图间隔限制，可设置两次生图开始之间的最短秒数，并在冷却期间禁用生成按钮。
- 图片编辑与重绘新增魔棒：可用 EfficientViT-SAM 智能选择点击位置的对象并抠除或生成蒙版，也可切换为无需模型的连续颜色区域选择。
- 生成历史新增可选的“官网式联动”点击行为：单击切换中央预览，可用左右方向键跨当前批次与历史图片浏览，并通过双击、长按或右键打开详情。
- 新增提示词助手配置与浮层，支持在提示词编辑过程中调用已配置的模型。

### 🛠 改进

- Danbooru 与 Gelbooru API 凭据改用系统安全存储；现有 Danbooru 登录信息会在安全写入并回读成功后自动迁移，Gelbooru 收藏会明确显示只读状态和帖子 ID 排序规则。
- 未设置过生成页布局的用户默认使用官网式布局。
- 历史图片在经典与联动两种模式下都支持 Ctrl/Command 快速多选；旧快捷键配置会自动补齐预览上一张和下一张操作，且不覆盖用户的现有绑定。
- 历史与生成预览图拖入启动器内部功能时会保留 NovelAI 元数据；拖到外部应用仍按“复制和拖拽时移除元数据”设置处理。
- Vibe Transfer 的编码、导入导出和库管理流程得到统一，兼容更多官网式文件与模型信息。
- 改进生成历史、图片预览、Image2Image、局部重绘、Precise Reference 和拖拽交互，并补充大量回归测试。

### 🐛 修复

- 修复 Gelbooru 在线画廊加载缩略图时可能掉帧，以及详情页未显示帖子标签的问题。
- 修复开启“SD语法自动转换”后普通圆括号和方括号被改写为数值权重的问题；现在只转换带数值的显式 SD 权重，并保留原有括号语法。
- 修复前导小数和嵌套内容的显式 SD 权重未被自动转换的问题。
- 修复随机替换、六角色和图片元数据导入后，角色位置画布与实际生成坐标可能不一致的问题。
- 修复窄画布拖动角色时可能报错，以及生成、报错、切换非 V4 模型或清空角色后位置画布仍遮挡预览的问题。
- 修复官网式布局打开角色位置画布会清除经典布局最大化偏好，以及清空提示词后输入焦点丢失的问题。
- 修复外拖保护开启时解码失败的异常图片可能以原始字节被复制或拖出的问题；现在会拒绝该操作，并在拖拽过程中使用不含原图的固定占位反馈。
- 修复局部重绘请求的模型、强度和尺寸参数与 NovelAI 官方接口不一致的问题。

### 👥 贡献者

- 感谢 [@rabiarabbit](https://github.com/rabiarabbit) 通过 [PR #63](https://github.com/Aaalice233/Aaalice_NAI_Launcher/pull/63) 贡献本版本的主要功能、改进和修复。

## [1.1.0] - 2026-07-17

### ✨ 新增

- 图片编辑器新增可再编辑的 3D 模型图层：支持程序化人偶和外部模型、模型与姿势调整、相机编辑、撤销、透明渲染以及双击图层继续编辑。
- 生成页新增可持久化的官网式布局，整合单列参数、模型选择、固定标签与生成控件，可在设置中切换回经典布局。
- 图片编辑与重绘新增输出分辨率控制，使用 Pica-compatible Lanczos3 缩放图像，并同步调整蒙版和 Focus 区域。
- 本地画廊右键菜单可将图片发送到 Image2Image、反推、Vibe Transfer、Precise Reference 和 Krita，并支持直接导入图片元数据。
- 提示词编辑器新增可选的鼠标滚轮调权功能，可仅调整当前选中的提示词权重。

### 🛠 改进

- 统一 Focused Inpaint、普通 Inpaint、流式预览、历史记录和 Krita 桥接的尺寸、蒙版、透明 patch 与最终合成语义，减少预览与实际成图不一致。
- 设置页重组为 9 个分类，新增“隐私与分享”和“集成”分区，并改进英文界面与附属卡片布局。
- 生成预览支持拖拽，并改进右键复制、Upscale 后预览替换和历史记录宽高比保留。
- 恢复账号密码登录入口，同时在界面中优先推荐更稳定的 API Token 登录。
- Windows 开发与发布流程新增 NuGet CLI 安装说明和显式校验，避免干净环境中因缺少 WebView2 构建前置而直接失败。

### 🐛 修复

- 修复 Focused Inpaint 选区、上下文裁剪、实际请求尺寸、Anlas 估算和结果贴回之间的偏差。
- 修复成图信息可能显示请求裁剪尺寸而非实际编码图片尺寸的问题。
- 修复 Windows Accessibility Bridge 可能导致应用崩溃的问题。
- 修复多词加权标签高亮、固定标签侧栏滚动条和多种生成预览交互问题。

### 👥 贡献者

- 感谢 [@rabiarabbit](https://github.com/rabiarabbit) 通过 [PR #59](https://github.com/Aaalice233/Aaalice_NAI_Launcher/pull/59) 实现并整理本版本的主要功能与修复。

## [1.0.1] - 2026-07-06

### ✨ 新增

- 新增日语界面与 NovelAI 术语本地化，语言选择和生成相关文案覆盖日语。
- Vibe Transfer 与 Precise Reference 支持逐项启用或停用，停用项不会参与生成请求、冲突判断或 Anlas 估算。
- 图片编辑器支持拖放导入图片，便于把外部图片直接作为编辑素材使用。
- Vibe 库支持 zip 导出与更完整的批量导出流程，导入和导出提示补齐本地化。

### 🛠 改进

- 优化本地图库和在线图库：提升收藏过滤性能，修复本地收藏计数、拖拽和过滤稳定性，并改进 Gelbooru 解析和视频预览。
- 强化启动、数据库和崩溃诊断：启动时检查核心资源数量，减少资产数据库重复加载，遇到 Hive、数据库或 metadata isolate 异常时尝试恢复或降级处理。
- 优化反推和 ONNX Tagger：降低图片反推内存峰值，兼容 CLTagger v2 模型描述和外部数据加载，并增加本地 ONNX 文件夹入口。
- 改进图生图、局部重绘和参考图链路：大图导入减少阻塞，多 sample 主预览更稳定，Precise Reference 尺寸处理更贴近官方行为，并增加 Focused Inpaint Anlas 成本提示。
- 拆分本地图库、Vibe 库和图片编辑器等大文件，补充关键回归测试，降低后续维护风险。

### 🐛 修复

- 修复 NovelAI 用户订阅和资料端点迁移后 Token 登录、Anlas 和会员状态可能刷新失败的问题；官方用户数据请求改走 `image.novelai.net`，第三方兼容站点继续使用自定义 main host。
- 修复自动登录成功后可能未保持自动登录开关的问题。
- 修复 Vibe 或图片元数据导入时禁用的 Variety+ 状态可能被旧状态覆盖的问题。
- 修复启动预热、数据库连接释放、ComfyUI 预览帧、图片解码和 Windows 单实例启动相关的稳定性问题。
- 修复 Release 页面发布说明中文件列表生成不准确的问题。

## [1.0.0] - 2026-07-06

### ✨ 新增

- 新增 Windows 安装版，安装到当前用户目录，支持开始菜单入口、卸载入口和应用内一键更新。
- 新增 Windows 便携版与 macOS 便携版发布包，用户可以按自己的使用方式选择安装或解压运行。
- 新增生成页设置项，可隐藏“随机提示词”和“抽卡模式”入口，避免误触后覆盖当前提示词工作区。

### 🛠 改进

- 更新检查改为识别安装版与便携版：Windows 安装版可下载并安装更新，Windows 便携版和 macOS 便携版只跳转 Release 页面。
- 登录页默认使用 Token 登录；账号密码登录入口暂时置灰，并用面向用户的说明引导改用 Token。
- Windows 应用图标改为透明背景的多尺寸图标，保留像素边缘并改善桌面快捷方式显示清晰度。
- 发布流程会自动生成下载文件说明、校验文件和 Release 页面内容，减少用户选错包的概率。
- 版本比较现在能正确处理稳定版、预发布版本和构建号，避免新版本检测结果失真。

### 🐛 修复

- 修复 Token 登录后无法正确获取点数与会员状态的问题。
- 修复 Token 校验接口变更后可能被误判为登录失败的问题。
- 修复启动预热进度中偶尔显示原始文本键名或残留上一条子任务文本的问题。
- 修正预发布版本更新筛选逻辑，未开启预发布时只检查稳定 Release。
- 修复 NSIS 脚本中文文案在部分环境下被按 ANSI 读取导致安装包构建失败的问题。

## [1.0.0-beta13] - 2026-06-20

本次更新新增 macOS 最小适配，并开始同时发布 Windows 与 macOS 压缩包。

### Added

- 新增 macOS 桌面端最小适配，支持启动、登录、本地数据库、在线图库视频、系统代理、Keychain 安全存储、图片复制和文件定位（[#52](https://github.com/Aaalice233/Aaalice_NAI_Launcher/pull/52) by [@Miint-Sunny](https://github.com/Miint-Sunny)）。
- 新增 macOS 应用图标、构建脚本和本地签名辅助脚本，方便贡献者在 macOS 上构建与调试（[#52](https://github.com/Aaalice233/Aaalice_NAI_Launcher/pull/52) by [@Miint-Sunny](https://github.com/Miint-Sunny)）。
- 新增 GitHub Actions 双平台发布流程，推送 `v*` tag 后会构建 Windows 和 macOS 压缩包并上传到同一个 Release。

### Changed

- macOS bundle id 调整为 `com.aaalice.naiLauncher`，避免后续正式签名或公证时继续使用 `com.example` 和下划线标识。
- README 已按当前功能、平台状态、构建步骤和发布流程整体刷新。

### Fixed

- 修复复制 JPG、JPEG 或 WebP 图片时可能被错误当作 PNG 写入剪贴板的问题；复制前会统一生成可粘贴的 PNG 数据，同时保留现有的元数据剥离设置（[#52](https://github.com/Aaalice233/Aaalice_NAI_Launcher/pull/52) by [@Miint-Sunny](https://github.com/Miint-Sunny)）。

## [1.0.0-beta12] - 2026-06-20

本次为 Windows 端大版本更新，汇总 `v1.0.0-beta7` 之后的主要改动，重点补齐图生图、局部重绘、扩图、本地工作流、Krita 联动、Vibe、提示词编辑和生成稳定性。

### Added

- **完整图生图与局部重绘工作流**：补齐图生图、局部重绘、Focused Inpaint、Outpaint 边缘扩展、点击式填充桶和虚拟画布扩图流程。
- **ComfyUI 本地工作流**：新增本地 ComfyUI 接入、工作流导入、内置超分和相关设置入口。
- **Krita Bridge 联动**：新增 Launcher 与 Krita 的本地桥接工作流，支持从生成结果、历史记录和图库发送图片到 Krita。
- **提示词 Token 明细**：正向/负向提示词支持 Token 计数、组成明细和紧凑显示。
- **提示词框内搜索**：长提示词现在可以直接在编辑器内搜索和定位。
- **Vibe 导入导出增强**：支持非 PNG Vibe 导入、Vibe 整包导入参数保留、PNG 元数据嵌入导出和批量导出。
- **精准参考增强**：Precise Reference 支持多图导入，拖放角色参考图时会追加到已有参考列表。
- **随机配置模式**：支持官方、自定义、混合模式持久化，并兼容旧随机预设。
- **本地图库多选增强**：本地图库支持当前页/全部结果两种多选范围，历史记录增加更多图片目标快捷操作。
- **反推入口补充**：反推可以作为全局拖图目标，并打通更多图片发送入口。

### Changed

- **生成取消逻辑更清晰**：连续生成时区分“跳过当前批次”和“停止全部”，避免误以为全部任务已停止。
- **批量生成更稳定**：重复生成和多样本批次分开处理，减少进度、取消和结果回写混乱。
- **流式生成失败更好排查**：生成失败或取消时会保留最后预览快照和元数据，方便进入详情查看。
- **图像编辑体验更一致**：局部重绘、扩图、硬边蒙版导出和预览图操作进一步对齐实际发送结果。
- **元数据兼容性提升**：保存、读取、导入和复用图片参数时更贴近 NovelAI 原生格式。
- **自动补全更贴合 NAI 语法**：自动补全会保留提示词外层语法包裹，减少权重或括号结构被破坏。
- **提示词助手更稳**：图片输入会在发送前自动压缩和规范化，降低请求失败概率。
- **图库与大图预览性能优化**：优化缩略图、缓存、Vibe 库导入和大图处理，减少卡顿。
- **Windows 文件定位更可靠**：优化“在文件夹中显示”相关操作，减少路径打开失败。
- **源码构建环境更新**：最低要求调整为 Flutter 3.35.0 / Dart 3.10.7。

### Fixed

- 修复生成取消链路中的多类问题，包括旧结果回写、空结果、取消后恢复异常和批量取消状态不明确。
- 修复局部重绘蒙版偏移、丢边、预览与发送不一致、扩图 frame 校验等问题。
- 修复 NovelAI 超分、变体、Focused Inpaint 等场景下的 Anlas 估算偏差。
- 修复本地图库逗号搜索、分类删除、图片元数据覆盖和部分搜索状态不同步问题。
- 修复 Vibe 库周期性卡顿、Vibe PNG 元数据回写和导出流程本地化缺失。
- 修复词库搜索框内全选快捷键被页面快捷键抢走的问题。
- 修复固定词侧栏中容易误触的权重快捷操作。
- 修复英文界面中部分中文文本泄漏的问题。
- 修复 ComfyUI 地址、模型解析、工作流导入和执行中的多处兼容问题。
- 修复 Krita Bridge 预检、安装、发送图片和局部重绘返回中的稳定性问题。

### Contributors

- 感谢 [@rabiarabbit](https://github.com/rabiarabbit) 回流并整理本次主要更新。

## [1.0.0-beta11] - 2026-06-13

本次更新基于 PR #8-#16 合并整理。

### Feature

1. 新增 Krita Bridge 本地桥接工作流，支持 Launcher 侧认证 WebSocket 服务、Krita 插件、设置页开关、生成/图库发送到 Krita、插件安装/打包/预检和验收脚本。
2. 完善图像编辑与重绘/扩图流程，新增 Outpaint 边缘扩展、虚拟画布扩图状态、导出时物化扩图源、硬边蒙版导出器和 CPU/Isolate 处理路径。
3. 增强参考图与图片输入能力，精准参考支持多图导入，生成页支持从本地拖放图片到 Vibe Transfer 与 Precise Reference。
4. 完成随机配置模式，支持官方/自定义/混合模式持久化、随机预设规范路由和旧预设兼容。
5. 提示词助手会在发送前自动压缩过大的图片输入，并保持刷新过程中的 Token 统计可见。
6. Vibe 整包导入会保留子项参数与原始图片来源，便于后续详情查看、编辑和生成复用。
7. 本地图库增加当前页/全部结果两种多选范围，历史记录增加图片目标快捷操作，反推也可作为全局拖图目标。
8. 生成过程中桌面端取消按钮始终可见，并在批量生成时显示进度；流式生成失败或取消时会保留最后预览快照和元数据，便于进详情排查。

### Bugfix

1. 修复生成取消链路中的多类竞态，包括取消后旧结果回写、空 fallback 结果、预览前未取消 stream、批量取消失败未暴露，以及取消中的请求恢复问题。
2. 修复 NovelAI inpaint 蒙版合成与预览图操作一致性，避免局部重绘蒙版在发送、预览或导出时偏移、丢边或被错误合成。
3. 修复图像编辑器虚拟扩图与硬边蒙版导出问题，包括 frame snapping、物化 frame 校验、图层 offset、基础蒙版合成和 DTO 字节可变性。
4. 修复本地图库逗号搜索筛选、在线图库搜索和自动补全细节，并优化大批量图像处理与 mask region fill 性能。
5. 修复 Vibe 库导入确认卡顿、Vibe PNG 元数据回写、导出流程中文本地化缺失，以及紧凑操作栏溢出问题。
6. 修复可见中文字符串在英文界面泄漏的问题，并补齐设置、Vibe、队列、快捷键、拖放、详情页等多个界面的本地化文本。
7. 修复 Krita Bridge 预检包输出路径、局部重绘返回速度和释放安全性问题，减少外部工作流联动中的失败与卡顿。
8. 修复元数据导入中 disabled Variety+ 状态、图片元数据覆盖、文件夹显示入口和若干生命周期回归，并增加吞掉异常的日志记录以便排查。

## [1.0.0-beta10] - 2026-05-09

### 更新日志

1. 新增固定标签侧栏，支持正向/负向标签分栏、条目链接和更完整的侧栏编辑流程
2. 优化固定标签弹窗与生成页布局，提升宽屏、窄屏和主工作区之间的协同体验
3. 修复 SD 与 NAI 混合语法转换问题，避免字面括号和权重括号被错误处理
4. 修复元数据导入与提示词语义恢复问题，提升旧图、外部图和应用内保存图的回读一致性
5. 在线画廊下载改为优先获取原图，并补充安全保存路径，减少非原图下载和覆盖风险
6. 修复 Windows “在文件夹中显示”路径打开问题，改用更可靠的系统 Shell 链路
7. 改进提示词 Token 统计、质量/UC 预设解析和参数面板交互细节
8. 补充 Krita 集成设计文档，为后续图像工作流联动预留实现方案
9. 增加固定标签、布局状态、参数处理、图片保存、元数据和语法转换相关回归测试

## [1.0.0-beta9] - 2026-05-04

### 更新日志

1. 修复了精准参考图片导入及生成时的卡顿问题
2. 优化了vibe的保存逻辑，现在保存到库的vibe可以长效保存
3. 修复了图片元数据保存/读取的一系列问题
4. LLM助手增加anthropic, openai response, gemini, deepseek, LM studio协议
5. LLM助手增加自定义功能，可以在提示词框右下角向LLM提出自由需求
6. 优化SD语法转换功能，现在会修复括号转义
7. 修复历史记录图片无法正确打开所在文件夹的问题
8. 历史记录图片增加收藏
9. 修复本地画廊自定义分类删除无效的问题
10. 优化部分UI/UX
11. 修复角色提示词的显示问题

## [1.0.0-beta8] - 2026-05-02

### Added

- **词库中文搜索反查** - 中文搜索词现在可以通过翻译数据库定位对应英文 Danbooru tag，并保留分类与热度排序信息。
- **完整图生图与重绘工作流** - 补齐 `img2img`、局部重绘、变体、增强等生成流程，并引入 `Focused Inpaint`、`Minimum Context Area` 与点击式填充桶。
- **可持续编辑的重绘画布** - 重绘编辑器支持再次进入后继续查看和修改已有蒙版，Focused 双框可视化与主蒙版编辑逻辑统一。
- **ComfyUI 本地工作流与内置超分** - 新增 ComfyUI 集成、工作流导入、设置页与内置 `SeedVR2` 超分，可直接在应用内切换本地超分后端。
- **导演工具独立工作流** - 新增导演工具页面与入口，支持从历史图、预览图和图生图源图直接送入增强处理。
- **提示词 Token 计数与明细** - 为正向和负向提示词增加 Token 计数、组成明细与紧凑模式显示，并纳入固定词、质量预设、UC 预设和角色提示词。
- **复制/拖拽元数据控制** - 新增“复制/拖拽时移除全部元数据”设置，同时保留本地保存图片的完整元数据。
- **Vibe 导入导出增强** - 支持非 PNG 的 Vibe 导入，并新增带 PNG 元数据嵌入的 Vibe 导出流程。
- **反推模块与本地 Tagger 工作流** - 新增“反推”链路入口，支持 ONNX Tagger 接入、结果回填提示词，并打通图库/画布图片送入反推。
- **多任务 LLM 助手路由** - 助手补齐优化、翻译、反推、角色替换任务路由，新增对应规则管理与提示词框注入入口。
- **保护模式细分开关** - 设置页新增保护模式并拆分子开关，覆盖拖拽净化、危险操作确认、外发提醒与高消耗预警。
- **本地 Effects 工作流补充** - 图像编辑区域新增本地效果处理入口，支持更完整的本地后处理操作。

### Changed

- **Vibe 导出流程更清晰** - 单个 Vibe 可直接另存为文件或嵌入 PNG，批量导出改为先选择目录并自动避免文件名覆盖。
- **元数据格式更贴近 NovelAI** - 保存与解析逻辑更贴近 NovelAI 原生风格，网页端拖入兼容性更好，同时应用自身仍能正确回读设置。
- **应用内拖拽与资源管理器拖拽对齐** - 在保留元数据时，应用内拖拽改为优先使用真实落盘文件字节，减少与资源管理器拖拽行为不一致的问题。
- **本地超分参数默认更保守** - 内置 `SeedVR2` 本地超分默认切到 `3B Q4` 模型与 `1.5x` 倍率，并把超分/增强参数改为和常规生成参数一样持久化。
- **历史记录交互更完整** - 历史列表与预览卡片的悬停栏、右键菜单现在都能直接进入局部重绘和放大。
- **SD 语法自动转换职责收敛** - `SD语法自动转换` 现在只负责将 SD 权重语法转换为 NAI 语法，空格转下划线统一交给自动格式化处理。
- **超分后端收口到 ComfyUI** - 移除独立本地 ONNX 放大路径，统一由 ComfyUI 工作流承载 `SeedVR2` 与常规超分模型选择与执行。
- **LLM 请求策略收敛为非流式** - 关闭流式输出路径，移除温度/Top-P/Max Tokens UI 配置并回退云端默认参数，减少截断和空响应问题。
- **提示词编辑撤销重做链路强化** - 注入式改写（助手/反推）统一接入可撤销历史，补齐 `Ctrl+Y` 重做行为。

### Fixed

- **修复词库搜索框状态不同步** - 工具栏重建后会重新同步当前搜索条件，避免输入框显示和实际筛选状态不一致。
- **修复批量 Vibe PNG 嵌入误入口** - 批量导出不再显示仅适用于单个 Vibe 的 PNG 嵌入选项，避免无效操作路径。
- **修复重绘灰块、灰边和蒙版显示异常** - 解决普通重绘与 Focused Inpaint 中的灰块、灰边、整块回贴、蒙版层级错误与清空后继续编辑不可见等旧问题。
- **修复 Vibe 库周期性卡顿** - 收敛 Vibe 库后台刷新、从库导入与缩略图解码路径带来的周期性卡顿与短时冻结。
- **修复生成页稳定性问题** - 解决冷启动切换负面提示词可能闪退、自定义尺寸输入被覆盖、更新检查 race 和缩略图失败重试风暴等旧问题。
- **修复大图预览性能问题** - 历史记录、画廊、精准参考等区域改为更节制的图像解码策略，降低大图场景下的卡顿。
- **修复 NovelAI 与本地超分结果不一致问题** - 解决 NovelAI 超分缺少倍率参数、ComfyUI 模型列表被错误解析成 `COMBO`、放大结果排序和详情分辨率错误的问题。
- **修复 Anlas 估算偏差** - 重新按用户订阅等级、实际请求尺寸和工作流模式计算 `Anlas`，覆盖 NovelAI 超分、变体和 `Focused Inpaint` 的真实消耗。
- **修复 ComfyUI 地址格式导致的假连接失败** - 统一规范化 base URL 与 WebSocket 路径，避免 `//ws` 等异常导致的任务执行失败。
- **修复本地图库送图链路缺失** - 打通本地图库卡片与详情页到 `img2img` 的发送入口，并补齐通用图片操作路径。
- **修复 Vibe 导入阶段全量读取** - 导入流程去除 `getAllEntries()` 重负载路径，改为按需查找与轻量读写。
- **修复 Vibe 展示缓存过重导致的首开卡顿** - 展示缓存升级为轻量 `v2`，剥离大字段并改为按需懒加载缩略图。
- **修复大体积元数据缓存后台清理卡顿** - 为超大 L2 Hive 缓存增加自动清理硬阈值保护，避免后台全量扫描/compact 触发长卡顿。

## [1.0.0-beta7] - 2026-03-07

### Changed

- **统一图像保存位置** - 生成结果卡片上的保存按钮现在会使用你在设置中配置的保存目录，和自动保存路径保持一致。
- **改进元数据兼容性** - 新保存图片的元数据结构已对齐 NovelAI 官方格式，网页解析识别成功率更高。

### Fixed

- **修复发布被远端拒绝的问题** - 构建产物 ZIP 不再进入仓库历史，发布流程更稳定。

## [1.0.0-beta5] - 2026-03-01

### Added

- **全局字体大小调整** - 在设置 > 外观中新增字体大小调节选项（70%-150%），支持实时预览和对话框精细调节
- **自动更新检查** - 添加应用更新检查功能，支持检测新版本并在设置页面显示更新提示

## [1.0.0-beta4.3] - 2026-03-01

### Fixed

- **修复窗口状态保存** - 修复窗口状态保存日志格式问题
- **修复背景色** - 修正窗口背景色显示

## [1.0.0-beta4.2] - 2026-03-01

### Added

- **词库分组视图** - 全新的分组视图模式，按类别组织词库条目，支持升序/降序排序
- **词库预览图自定义** - 支持调整词库条目的预览图显示范围，包括偏移和缩放控制，提供可视化裁剪对话框
- **词库 Toolbar 改造** - 新增视图切换按钮（平铺/分组/紧凑）和排序下拉菜单，操作更便捷
- **悬浮按钮动效** - 词库条目卡片悬浮按钮添加平滑动画和 Tooltip 提示

### Changed

- **优化快捷键处理** - 统一快捷键处理逻辑，修复快捷键配置变化后未能正确重建的问题
- **生成页面组件化** - 重构桌面端生成页面布局，提取为独立的可复用组件
- **词库条目卡片交互优化** - 改进背景与边框层级，修复悬浮按钮事件响应问题

### Fixed

- **修复 Sliver 布局错误** - 解决 SliverPersistentHeader 的 layoutExtent 超过 paintExtent 的报错
- **修复画廊路径冲突** - 解决图片移动后路径唯一约束冲突问题
- **修复预览图布局** - 修复 ThumbnailDisplay 和 ThumbnailCropDialog 的布局和缩放问题
